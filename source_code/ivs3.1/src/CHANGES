# HELLO packet removed. Now a DESC packet is automatically sent when a
new station appeared if feedback is allowed.

# NACKs bug fixed. A FIR was sent at each loss...

# -r option bug fixed.

# The option -I allows to specify a new control port. Useful to differenciate
several ivs conferences in the same machine.

In progress:

# New DESC format packet, abilibity to change the session name during the 
conference.

# resequencement.

# Rate control according to the network conditions.